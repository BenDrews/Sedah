using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Attack : EntityState
{
    public Attack(EntityStateMachine entityStateMachine) : base(entityStateMachine)
    {
    }
        // Start is called before the first frame update
        void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
